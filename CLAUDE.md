# Crystal Command

> **RENAME PENDING (2026-07-23): "Broodfall" is the front-runner** — "Crystal
> Command" collides with an existing Steam game and the Crystal Commanders
> Quest RTS. Full vet + market strategy: `BROODFALL-BRIEF.md`. Do NOT create
> the App Store Connect record (ship-checklist ap2) until the name is locked.
> Rule regardless of outcome: never use the phrase "brood war" in marketing.

A tiny browser RTS in the spirit of Command & Conquer / StarCraft 2, built for and with Bronson (a big fan of those games). Pure canvas + vanilla JS, zero dependencies, runs from `file://` by double-clicking `index.html`.

- **Repo:** https://github.com/bronsongannon/crystal-command (public)
- **Live / shareable:** https://bronsongannon.github.io/crystal-command/ — GitHub Pages serves the `main` branch root, so **every push to `main` auto-deploys** (~1 min).

## Files

- `index.html` — page shell, all CSS, UI DOM (top bar, minimap, command card, help, toasts, game-over overlay)
- `game.js` — the entire game (~1,300 lines): data tables, fog of war, economy, combat, enemy AI, input, rendering, sprite FX, WebAudio sfx
- `assets/fx/` — Kenney CC0 particle sprites (explosion/smoke/puff variants + two muzzle-flash shapes), downscaled from the source packs. Loaded by the `loadSprites` block in `game.js`; if any image fails, `spritesReady` stays false and effects fall back to the original procedural drawing (game still works from a bare `game.js` + `index.html`).
- `assets/sfx/` — OPTIONAL sample sfx slots (2026-07-11): drop `<name>.wav|ogg|mp3` matching a `SFX_NAMES` entry and `playSfx()` plays it instead of the beep — each file independent, missing = beep fallback (same philosophy as OPT sprites). Loads via `<audio>` elements so it works from `file://`. Per-slot volume in `SFX_VOL`; 4-element pool per sound for overlap; slot list + sourcing guide in `assets/sfx/README.md`. Bronson is sourcing sounds (pointed at Kenney CC0 / Sonniss GDC / OpenGameArt / Freesound CC0).
- `assets/sprites/` — Kenney CC0 body sprites for all units & buildings (tanks pack + Top-Down Shooter infantry + Tower Defense building plates/turret gun). Loaded by `loadBodies`; `bodiesReady` gates sprite vs procedural rendering the same way. Team coloring is done at load via `teamSprite()` (multiply-tint cache) — art is neutral sand/khaki, tinted teal/red per team.
- `README.md` — player-facing docs (controls, units); keep it updated when gameplay changes

## Game snapshot (as of 2026-07-09)

- **App icon (2026-07-22, ship-checklist ap4):** `mac/icon/` — `master.png` (1024) is the single source; `make-icon.py` emits the full `AppIcon.appiconset` into `mac/CrystalCommand/Assets.xcassets` (wired via `ASSETCATALOG_COMPILER_APPICON_NAME`). Current master composed by `make-placeholder.py` from the game's own `crystal.png` (Apple icon-grid squircle 832/1024, radius 186; padded-canvas Gaussian glow — unpadded blur clips to the sprite bbox and reads as a dark rectangle). Verified in the Dock. Upgrade = drop new master + rerun script; Gemini prompt (crystal + raptor eye-shine concept) and Fiverr brief in `mac/icon/README.md`. GOTCHA: LaunchServices caches Dock icons per bundle PATH — rebuilding in place shows the stale/generic icon; launch from a fresh path or `lsregister -f` + `killall Dock`. Store installs unaffected.
- **Mac wrapper (2026-07-22, ship-checklist ap3):** `mac/` — hand-authored Xcode project (objectVersion 77, synchronized folder group), two Swift files (`main.swift` programmatic entry + `AppDelegate.swift`: WKWebView window, programmatic menu bar with Game/Help menus bridged into the game's JS via evaluateJavaScript, fullscreen, close-quits). "Bundle Game Files" script phase rsyncs index.html/game.js/assets into `Resources/game` every build (excludes Source dirs/md/tsv; ~19MB app). **GOTCHA (cost an hour): a sandboxed WKWebView app MUST have `com.apple.security.network.client` — without it WebContent crashes instantly and the window is black, even for purely local content, with nothing in the logs.** Signed with Bronson's Apple Development identity (team X22M9K8T66), bundle id `com.bronsongannon.crystalcommand` — must match the App Store Connect record (ap2, still open). Debug builds: inspectable + load-failure title diagnostics. Verified inside the sandboxed .app: skirmish sim, sprites, all 18 sfx, localStorage across relaunch (smoke title `launch:2 units:16 sprites:true sfx:18`). Details + remaining submission steps: `mac/README.md`. `mac/build/` gitignored.

- **Dialogue PiP + voice pipeline (2026-07-16):** the dialogue bar is now portrait + text (`#dlg-pip` comms panel: faction-color border, scanline overlay, animated waveform while `.talking`; monogram fallback via `CAST[who].init`). Art slots: `assets/portraits/<who>.png` (ops/sci/red, square bust ~256px, spec in that folder's README). **Voice lines:** `assets/voice/<who>_<hash8>.mp3|ogg|wav` — `voiceKey(who,text)` (djb2 of `who|text`, so rewording orphans the clip — deliberate), probed lazily per mission (`preloadVoices` in missionInit + openBriefing), each clip independent, missing = silent typewriter. Playback: voiced lines hold the bar for clip length (text pacing is the floor); briefing lines wait for their clip (guarded on `clip.paused` so a blocked/missing clip can't wedge the reveal); rush-cut/skip/quit/mute all `stopVoice()` (no overlap, ever); pause + help modal freeze voice via `syncVoicePause()`. `CC.exportVoiceScript()` downloads `voice-script.tsv` (mission/context/speaker/filename/line, deduped) — the studio script for whatever AI voice engine Bronson picks (exploring Higgsfield vs a dedicated TTS; advice given: pre-bake audio, never runtime TTS — offline file:// + "collects nothing" privacy policy). Verified: PiP + fallback + voiced line + clip-cut + skirmish regression, zero console errors. **First real content landed 2026-07-21, both via DaVinci:** Vega's briefing-opener clip (`ops_d9cb6395.mp3`, 9.6s) and her portrait (`ops.png` — bust-crop the raw art to head-and-shoulders before installing, a full waist-up frame is unreadable at 58px; full-res original in assets/portraits/source/ for key art). **Cast portraits COMPLETE 2026-07-21** (all DaVinci film-stills, matched set): `ops.png` Vega, `sci.png` Lin (amber glasses + vial bandolier), `red.png` Krauss (Rubicon pennant chest badge — mirrors the in-game HQ pennant). All bust-cropped for the 58px PiP; full-res originals in `assets/portraits/source/` (store key-art material).

- **Loop:** pick map + difficulty at the start menu → harvest crystals → train army → survive escalating AI assault waves → crack dino nests to expand → destroy enemy HQ.
- **Team color pass (2026-07-12, CAMPAIGN.md item 2):** COLORS[3] = bone hide `#c2bb96` / moss dark / pale-bone light (wild dinos read as NATURE, not a faction; player-hatched spitters stay teal). Colorblind rule: teams separate by LUMINANCE — bone .72 / teal .59 / red .49 — documented at the COLORS table; keep future schemes (Broodfallen) on distinct brightness bands. `teamSprite(img, team, tint?)` now takes a tint override (cache keyed by tint); `bldSprite()` = structures tinted `COLORS[team].bld || main` — red's identity touch: buildings `#b8443a` darker than units (drawBuildingSprite uses bldSprite everywhere). Red HQ flies the Rubicon pennant (`drawRubiconBanner`, drawn in drawBuilding so sprite + procedural paths both get it). Spitter throat sac pinned `#a8d060` (venom = biology, not faction); egg speckles now moss (.dark). Xenobiology-green accents (capture ring/cage/specimen ring) deliberately unchanged. Bronson's follow-up: flat tint is "kind of boring" → **`assets/sprites/STYLE-GUIDE.md`** (2026-07-12) is the full spec: five-role palettes per team (hull/trim/accent/structure/fx, incl. Broodfallen preview) + per-character signature details + Phase B (code-only procedural overlays: visors, stripes, hazard chevrons, muzzle bands, awnings) and Phase C (`unit_<type>_trim.png` two-tone mask pipeline). **Phase B SHIPPED (2026-07-12):** COLORS has all five roles (main/trim/accent/bld/fx + HAZARD_YELLOW universal); `drawUnitDecor(u)` + `drawBuildingDecor(b)` draw each character's signature detail over sprite AND procedural bodies (own translate/rotate transform, +x = facing; called from all three drawUnit exit paths + drawBuilding). Player structures now deep teal (`COLORS[1].bld`). Tracer bolts use the team `fx` role. GOTCHA: decor coordinates are tuned to current sprite sizes (r×2.7 draws) — if unit art scale changes, revisit drawUnitDecor offsets. **Colorway slots SHIPPED (2026-07-12, replaces the trim-mask Phase C):** pre-colored art bypasses tinting — `optCW(base, team)` + `CW` map (`_teal`/`_red`/`_wild`); lookups in drawUnitSprite (incl. hunker), drawDino, drawBuildingSprite (incl. turret_gun overlay). Fallback chain: colorway art → tinted neutral art → procedural. **assets/sprites/STYLE-GUIDE.pdf** (26 pages, generated by `style_guide_pdf.py`) is the Gemini brief: per-character sheets with swatches + prompts. Workflow that WORKS (verified with real Gemini output): generate teal with the approved marine attached as style anchor → image-to-image RECOLOR to red (never regenerate — keeps sprites identical) → bg-remove → crop to sprite bounds (Gemini leaves ~45% margin!) → 256px PNG → drop in. First approved sprites: marine pair, then the medic pair (LIVE in-game 2026-07-12 — first colorway art shipped). Batch 2 (2026-07-12): rocket/engineer/harvester/sniper(+prone hunker) pairs. Batch 3 (2026-07-12): raider/tank/apc/artillery/rig/gunship/harrier pairs, spitter (wild+teal), and ALL buildings (hq/barracks/factory/supply/power/airpad/refinery/turret/flak/silo — some red versions pending). GOTCHA: Gemini ignores facing — check EVERY sprite (harvester faced left → rotate -90; raider + rig faced down → rotate 180). process_sprite.py doesn't auto-orient. **Death animation (2026-07-12):** `unit_<type>_death1..4_<cw>.png` slots — `animFrames(type, kind, team, max)` collects consecutive frames (memoized once non-empty; async-load safe); kill() spawns a `corpse` fx (fall frames at 9 ticks each + ~2s linger + fade, soft boom instead of fireball; fog-gated like other world fx). COMPLETE for all infantry both teams + all dinos (52 frames): marine/sniper/medic/engineer/rocket `_teal`+`_red` (2026-07-12), spitter `_teal`+`_wild` (2026-07-12), raptor `_wild` (2026-07-13 — corpse mass target scaled ×(34/24.3)² because the live raptor draws in an oversized box; keep-largest-component per frame since dino sheets have no dropped gear and the ✦ watermark can sit dimmer than the near-white filter). Slicers live in `assets/sprites/slice_death.py` (x-projection segmentation; grid sheets get gray-scrub) + `slice_cluster.py` (component clustering for sheets whose frames nearly touch — x-gaps can be ~2px); both slice the DEATH band only, normalize by opaque area to the unit's static colorway sprite, and kill the Gemini ✦ watermark (small near-white comps + neutral-white flood from transparency — tune the spread threshold per sheet: cream trim can be nearly neutral). Vehicles keep the fireball, aircraft skip — per Bronson. **Walk cycles round 2 (2026-07-20): SHIPPED for the marine via AI video.** Round 1 (sliced spritesheet frames, 2026-07-12) was reverted — too few frames read as flicker. Bronson then rejected both procedural options ("the wobble looks silly"; a translate weight-shift tried 2026-07-20 read worse) and generated a walk-in-place video with DaVinci AI → `assets/sprites/slice_walk.py` (stride-period autodetect on the leg band, bg flood + feet-area-only shadow flood — the gun is the same gray as the shadow, spatial split is the only reliable separator on h264, ONE union crop box so frames don't jitter, mass-normalized to the static sprite) → `unit_marine_walk1..8_teal.png`. Engine: walk slots auto-registered (1..8, all colorways), drawUnitSprite picks frame by `walkT/(WALK_STRIDE_PX/n)` — distance-driven so feet stay planted at any speed; infantry WITHOUT walk frames keep the original rotation sway fallback. **Colorway derivation (2026-07-21):** `recolor_walk.py <type> <src> <dst>` derives the second colorway from existing frames (color+position NN trained on the unit's pixel-aligned static pair, delta-shift keeps video shading, 5×5 median on the delta field kills region-boundary salt) — marine red, sniper red, rocket teal shipped this way; one video per BODY is enough. DaVinci videos still wanted: medic/engineer teal, spitter/raptor/critter wild. Slicer is bg-adaptive since the rocket video (light-gray bg: outline/shadow caps scale with bg luminance; crop pads, never clips). Video spec that worked: locked camera, straight top-down, walking in place facing up, plain background, 2-4s. Bronson approved the marine result ("this is perfect") and wants ALL walking units done — per-unit DaVinci prompts queued in **assets/sprites/WALK-VIDEOS.md** (infantry both colorways, then dinos; ask for NO drop shadow — slices cleaner). Sniper scope-glint decor no longer blinks (2026-07-20 — the blink read as unexplained muzzle flash); sniper prone stays hunker-only, which was already true (standing colorway art draws while walking). Sheet slicing lessons (for future death/idle sheets): Gemini sheets have grid lines + labels + mixed views — border flood-fill (TOL 60; the dark outline protects the sprite interior) → gray-scrub (low-sat, lum .60-.86 kills grid pockets, spares cream pauldrons) → keep-largest-component; normalize frames by opaque-AREA to the static sprite's mass (32800 @256px); TURNING/FIRING sheet sections are unusable (engine rotates freely; muzzle fx are game-drawn). **`assets/sprites/process_sprite.py`** turns a raw Gemini PNG into a game-ready slot in one command (`python3 assets/sprites/process_sprite.py "raw.png" unit_medic_teal.png`): border flood-fill bg removal (preserves interior whites — critical for the medic), drops floating islands (Gemini's ✦ watermark), crops to bounds, pads square, 256px. Raw drops live in `assets/sprites/source*/` folders.
- **Portrait direction (2026-07-14 Q&A, merged 2026-07-20):** portraits are **PHOTOREALISTIC** film-still style — Bronson's call, supersedes the painted-bust direction; NOT sprite style, keep the full-bleed bg, do NOT process_sprite.py them. Prompts + per-character voice briefs: **assets/sprites/PORTRAITS.md** (generate ONE portrait, approve, then style-anchor the other two — matched set). NOTE: the `#dlg-face` OPT-slot implementation this bullet originally described was superseded by the 2026-07-16 `#dlg-pip` panel — art now drops in `assets/portraits/<who>.png` (ops/sci/red), spec in that folder's README; PORTRAITS.md remains the prompt/voice source of truth.
- **Direction decisions (2026-07-14 Q&A with Bronson):** (1) mission LENGTH CURVE: M1-M4 ok at ~15 min; ramp toward a 30-min MINIMUM as the campaign progresses ("if not longer depending on the story"). (2) Escort/defense missions: close calls wanted, including "extremely close calls" — don't sand the tension off. (3) Lengthening early missions: enemy base or a lot more dinos both acceptable. (4) MAPS: all proposed biomes greenlit (snow, swamp, night, water) + varied terrain art + **ELEVATION** — "having it be flat the whole time becomes easy"; Bronson picked **Option A: vision high ground** (no damage modifier). **ELEVATION SHIPPED (2026-07-14):** MAPS `plateaus: [{c: [[x,y,r],…], ramps: [[x,y,r],…]}]` — interior tiles → `elev` grid (Uint8Array, MAP_W×MAP_H, filled in setup, `elevAt(wx,wy)`, on CC); rim auto-grows a chain of `cliff:true` rock slabs (r17 every 30px, gaps at ramps + sibling-disc seams) so pathing/collision/placement all reuse the rock machinery — ramps are the only ground route up. Rules: `stampVision` skips tiles above the viewer's elev (flyers pass `fly` → see everything); `nearestEnemyUnit/Building` skip targets on higher ground (flyer targets exempt, `fromAir` seekers exempt via acquireTarget) — so high shoots low, low can NOT auto-answer; explicit orders/retaliation pierce (player force-fire still needs fog visibility). Cliff slabs draw as tangential wedges (`rk.a` = outward normal), plateau interior gets a lifted-tone disc + drop shadow, ramps a half-lift disc (pal.hi overrides). First live use: valley's twin overlooks (N ramp west, S ramp east). Verified: elev grid, 49-slab rim, upward-vision block, no-auto-acquire-uphill (spitter on top freely shot a blind marine below), ramp A* both ordered + autonomous. GOTCHA: `elev.fill(0)` runs in setup unconditionally — stale grids from a previous map otherwise survive. 8-map roster: 4 existing + Trade Road (move M2 there), The Scar (Act 2), Overgrowth (Act 3), The Crater (finale); reused maps must relocate bases (mission-level pHQ/eHQ override, same pattern as M1 `fields`).
- **Queue UX round 2 (2026-07-14):** (1) **Click-eating fixed** — card/qpanel rebuild innerHTML when crystals cross a 25-step; a rebuild between mousedown and mouseup silently ate the click (felt like 2-5 clicks to queue anything). Dock delegation now fires on `pointerdown` (button 0 only) — immune to rebuilds, snappier. GOTCHA: any future panel with rebuilt-under-the-mouse buttons needs the same treatment. (2) **Construction rush** — selecting a building under construction (`built < 1`) shows a Constructing panel with the same fee model as unit queues: 2× speed = half building cost (`b.buildBoost`), finish-now = full cost (`rushConstruction`, `data-act crush:*`). Finish-now jumps to ONE TICK from done so completion side effects (refinery free harvester, toasts) still fire on the natural crossing. buildBoost multiplies both `built` accrual and the incremental hp gain (damage still sticks).
- **Playtest batch (2026-07-14, seven items):** (1) **Refinery-only economy** — `nearestDropoff` accepts refineries ONLY (HQ = command post; eggs/captives still go to HQ); every `placeBase` drops a starting refinery on the HQ→patch line (bare missions included — economy is dead without it); AI rebuilds a lost refinery with top priority; throttled '⚠ No refinery standing' toast when harvesters have nowhere to go. (2) **Crystal deposit sound removed** (too chatty; eggs/captives keep it). (3) **Finish-now stale progress bar** — the rush click handler forces refresh with `lastQSig=''` but an EMPTY queue also produced sig `''` → matched → never repainted; empty sig is now `'none'`. GOTCHA: '' is the force-refresh sentinel, real sigs must never equal it. (4) **Nest burst** — damaging a nest erupts 2-3 extra spitters (`NEST_BURST_CD` 30s, `e.burstAt`), spawned with `home=null` (not brood) + `roam=true`, ordered at the attacker; new order `'roam'` = fight what comes close (+dinoAggro), else wander (random headings, idle-case re-enters roam via `u.roam`). (5) **Grazers + dinoRage** — `UNIT.critter` (passive prop wildlife, campaign maps only: 8 spawned in setup when `mission`); NEVER auto-targeted (`type==='critter'` skip in nearestEnemyUnit — deliberate right-click kills only); each kill by team≠3 → `dinoRage++` → dino aggro/leash +25px each (cap 150) + nest respawns/den packs accelerate; reset in resetWorld; dinos without death frames get a soft ring, no fireball. (6) **Specimen half-rate replaces the hard weapons-lock** — soldiers CAN target/hit specimens now; `fire()` doubles `src.cool` vs specimen for team 1; splash/retaliation skips removed; M1 dialogue rewritten (harrier bomb + nuke exceptions unchanged... bomb skip REMOVED too? NO — bomb skip kept). M1's respawn nets now load-bearing. (7) **Vehicle steering** — wall-slide picks its ±90° side ONCE per building (`o._slideB/_slideS`, re-deciding every tick read as spin-out) + 6-tick hysteresis hold (kills the goal/slide shimmy) + `_slideT>45` → ghostT escape (buildings aren't in the A* grid so the watchdog never fired for them) + vehicles turn ≤0.22 rad/tick (infantry/dinos still snap) + A* watchdog 150→90 ticks.
- **Raptor + Raptor Den (2026-07-13, CAMPAIGN item 3 — the Act 1 finale swarm tease):** `UNIT.raptor` (fast melee pack hunter: speed 3.4, hp 65, range 16 "fake melee", `infBonus` 1.5 vs IS_INF — the flesh mirror of vehBonus; noAA) + `BLD.den` (1100hp, team 3). Melee is a fire() early-return: NO projectile — direct damage + `slash` claw fx + recoil-as-lunge; no muzzle flash. Den behavior (updateBuilding): spawns with `DEN_GUARDS` 2 door guards; every `DEN_PACK_EVERY` (50s) births `DEN_PACK_SIZE` (3) raptors attackmoving at the NEAREST standing structure of ANY faction (dens don't pick sides — M8's both-sides hunts come free); idle survivors flip to guard-at-den; hunts pause at `DEN_RAPTOR_CAP` (9). Alert pings the TARGET base, not the den (no fog leak). `IS_DINO` map now feeds isFlesh/isVehicle (raptors medic-healable, never engineer-repairable). Mission `bld:'den'`/`'nest'` spawns route through makeDen/makeNest so they come alive (guards/brood + hunt clock). aiExpansionSpot avoids dens like nests. Art: procedural raptor (whip tail, sickle legs, amber eyeshine) + den (burrow mouth w/ blinking eyeshine, trophy ribs, claw drags); OPT slots `unit_raptor[_wild/_teal/_red]`, `dino_den`, death-frame slots auto-registered. NOT on any skirmish map (Act 2 lever; M7 scripts the first one). CC.makeDen/spawnRaptor for testing. Verified: pack hunt vs player base, melee vs infantry/buildings, kill credit, cap, guard return, skirmish + M1 regressions clean.
- **Mission framework (2026-07-11):** `CAST` (named voices: ops=Cpt. Vega, sci=Dr. Lin, red=Cdr. Krauss) + `MISSIONS` table (pure data, same philosophy as MAPS) + mission engine (search "Mission engine" in game.js). Objective types: `unitCount`/`built`/`mined`/`reach` (any player unit within r of x,y)/`captive` (watches teams[1].captives)/`groupReach` (≥count of a spawn group within r — convoy escort)/`destroy` (no hostile building of type within r of x,y — nest cracks)/`flag`; `hidden` objectives appear via triggers; active undone objectives with `mark:[x,y]` draw pulsing teal beacons in-world above fog + blinking minimap rings (reach objectives auto-mark their own spot). Triggers: `{when:{time|done|notDone|groupDead|groupBelow:[group,n] (alive<n)|near:[x,y,r] (route progress)|noCaptive|mined}, delay?, repeat?, every? (secs cooldown between repeat fires — periodic waves), say?, objective? (id or array), complete?, spawn? (obj or array; units with order 'attackhq'|'guard' or to:[x,y] attackmove; bld:'type' pre-built structures — outposts), alarm?, crystals?, lose? (scripted defeat)}` — cloned into `ms` per run so specs stay pristine; `repeat` triggers re-verify `when` at fire time. Missions can also author extra crystal fields via `patches: [[x,y,n,amt]]`. Groups track units AND buildings (`groupAlive`). Mission flags: `noEnemy` (skips placeBase(2) + gates aiUpdate/waveUpdate + hides wave chip), `noWaves`, `bare` (player starts HQ+harvesters only). Win = `winWhen` id list all done (outro dialogue plays first); player HQ loss always = defeat (`checkEnd` early-returns to `missionEnd`). UI: start-menu mode tabs (Skirmish/Campaign, `cc.mode`), linear unlock (`cc.campaign` = missions completed), full-screen typewriter briefing (`#briefing`, click text to skip), objectives HUD (`#objectives`, top-left), dialogue bar (`#dialogue`, typewriter on sim tick so pause freezes it). Overlay button (`#btn-again`) is dual-mode: skirmish=reload, campaign=back to mission list. Testing: `CC.startMission(i)`, `CC.mission`, `CC.missionState`, `CC.unlockAll()`. Verified: full mission-1 win path (incl. capture + respawn nets), loss path, skirmish regression.
- **Capture Rig (2026-07-11, campaign unit):** `UNIT.rig` — unarmed harvester chassis + containment cage, NOT in any `trains` list (mission-granted via spawn triggers; give it a build slot later if skirmish wants it). Right-click a spitter with a rig selected → `order 'capture'` (chase → channel `RIG_CAP_TIME` 3s at `RIG_CAP_RANGE` 40 → target silently removed via hp=0, no death fx/kill credit → `u.captive=true`) → `'returnCaptive'` to own HQ → `teams[t].captives++` (objective type `captive` watches this). Excluded from `isCombat`; engineers repair it (`isVehicle`). Draws as harvester body + cage overlay (`drawRigCage`, glows green when loaded) + capture progress arc (`drawCapRing`); `unit_rig.png` OPT slot for real art.
- **Protected specimens (2026-07-11, playtest fix — marines were shredding the capture target):** spawn trigger `specimen: true` → `u.specimen`; team-1 weapons are HARD-LOCKED against it: skipped in `nearestEnemyUnit` (kills idle auto-acquire + attack-move + turrets), refused in `commandAttack`, no retaliation in `damage()`, and right-clicking it without a rig = move order. Escorts now soak the specimen's spit instead of murdering it. Pulsing green dashed ring in drawUnit reads "don't shoot". Wild non-specimen spitters unaffected.
- **Mission 1 "Landfall" (2026-07-11, reworked after playtest — exploration BEFORE combat):** basin, noEnemy+bare. Arc: econ (4th harvester) → base (depot → rax + 4 marines + turret) → **scout both fields** (Lin: "they only defend what they can see") → dinos SPOT the patrol → retaliation probe (repel) → rig + protected scout spitter spawn → capture + haul to HQ → mine 1000 (passive). Pacing pass 2026-07-14 (10-min playtest): mine 600→1000, and M1 now overrides the basin fields via mission `fields` (same spec shape as MAPS patches, REPLACES map patches in setup — any mission can use it): north field [W*.30, H*.22] + east field [W*.72, H*.62], nests on their far sides ~1900-2200px from the player HQ (was ~1350 mid-map). Playtest round 2 same day (match ran 10 min): (a) **green = interact** visual language — eggs draw a pulsing green dashed ring + blinking green minimap dot; player rigs ALWAYS wear the specimen's green ring (in drawCapRing, gated `type==='rig' && team===1`) so the rig↔specimen pairing reads at a glance, M1 rig dialogue names it ("the caged harvester wearing the green ring… the SAME green ring"); (b) **shy wildlife** — `wildSeen` flag (reset in resetWorld): roamers (grazers + loose spitters) within 480px of a standing player building get steered away in the roam case UNTIL the player has seen a team-3 unit in vision AWAY from base (`nearestPlayerBld` guard on the flip check — a sighting at the camp fence doesn't count, else approaching = permission). Verified: fence-dropped grazer flees to ~950px while shy, wanders to 69px after wild first contact. (c) **rig capture is specimen-only** — right-click routing requires `t.specimen` (was: any hostile spitter); rig + ordinary dino right-click = toast + escorts attack while the rig holds (unarmed rigs must never get commandAttack — they'd chase forever); the capture order case also bails on non-specimen targets (guards scripted/console orders; future capture missions like M16 must set `specimen: true` on capturable spawns). (d) shy-flee FIX (playtest: "a spitter wigged out at a building"): the flee point is sticky (`o.flee`, held until reached) — v1 re-aimed away from the NEAREST building and nulled `_path` every tick, so between two base buildings the heading whipsawed (spin-out). (e) **refinery crystal standoff** — aiSpotFree gives refineries a wider crystal gap (w/2+65 vs +26 for others); starting/AI refineries no longer land on the field's doorstep (player manual placement unchanged). (f) **per-map ground palettes** — MAPS `ground: {base, mottle, pebble, grid}` feeds paintGround(M); gauntlet=rust badlands, boneyard=cold ash, valley=deep moss, basin=classic. First lever toward the 8-map roster (Bronson: min 8 maps for 20 missions, distinct look/feel per mission, reused maps must relocate bases). Dinos never attack before the player is discovered (playtest: "they shouldn't be searching for us unless they see us"). Safety nets: repeat-triggers respawn a lost specimen (gated on `noCaptive`) and a lost rig.
- **Missions 2 + 3 (2026-07-12, BUILT):** M2 "Claim Jumpers" (basin, noEnemy): Krauss voice debut; 6-harvester convoy (group 'convoy', player-controlled) escorted to a pre-built NE outpost (mission `patches` + bld spawns) and home; `near`-triggered raider ambush + post-leg ambush + `every`-55s harasser; lose via `groupBelow:['convoy',4]` — both legs need 4+ alive. Lengthened 2026-07-14 (playtest: sub-8-min): NEW loading-siege beat at Beta — on 'out' done a 3-man garrison spawns + Krauss hits the post at +10s (3 raiders + tank), 'back' only reveals at +60s ("cargo aboard"), southern ambush beefed to 4+3 and re-timed to +70s (sets up AHEAD of the returning convoy), harasser every 55→45s. Verified hands-off: escorts+garrison hold the siege with 0 convoy losses; full win chain intact. GOTCHA: objective reveal = `o.active` (`.hidden` never mutates on the ms clone). Intro warns to swing EAST around the nest mounds (straight route walks into the south brood — deliberate danger, playtest-verified lethal). M3 "The Nest Problem" (valley, noEnemy): factory → 2 artillery → `destroy` the SE center nest (mark), while Rubicon theater squads (4 red marines, repeat every 55s, gated notDone nest) feed the NW nest forever on open comms; nest death drops eggs → hatch objective (existing unitCount spitter); mine 800. Lin's on-the-record objection + "something answered" outro seed Act 2. Verified: M2 win (convoy home 4/6) + lose paths, M3 full chain incl. theater + egg salvage, M1 + skirmish regressions.
- **Progressive build menu (2026-07-11, playtest fix):** locked buildings are HIDDEN from the command card (not 🔒-dimmed) — bare start shows ONLY Supply Depot; each tech unlock reveals more + fires a `🔓 Construction unlocked` toast (availability diff in refreshTopbar via `lastAvail`). power + refinery now `req: ['supply']` so the depot is the universal tier-0. Hotkeys on hidden buildings still toast the requirement via startPlacing.
- **Dialogue pacing (2026-07-11, playtest fix — commentary lagged ~30s behind fast builders):** `dlgDur` trimmed (2.2s + 0.035/char, cap 7s) and when the queue has a backlog the current line types at 2.6 chars/tick and holds only ~1s after finishing. Full 5-trigger tutorial burst drains in ~16s (was ~45s).
- **Full-code audit (2026-07-12, 2 review agents + targeted sweep + soak tests; all fixed & verified):** (1) constructing buildings silently healed all non-lethal damage (`max(hp, maxHp*built)` floor) — hp now accrues incrementally so damage STICKS; (2) specimen protection had splash holes — arc + harrier bomb loops now skip `u.specimen` for team 1 (nukes deliberately still hit — friendly fire is the nuke's brand); (3) nukes could launch from a dead silo — `launchNuke` requires a standing silo and `kill()` clears `nukeTargeting`; (4) **tutorial soft-lock**: a rig/harvester interrupted mid-haul stranded its captive/egg forever (returnCaptive/returnEgg only reachable from capture/collect) — idle case now auto-resumes delivery when `captive`/`eggCarry`; (5) units killed mid-tick (splash, capture) acted for one tick as corpses — `updateUnit` now early-returns on hp≤0 (mirrors updateBuilding); (6) `objMet` counted dead units (missionUpdate runs before the corpse sweep) — unitCount/built/reach now filter hp>0; (7) game-over overlay `setTimeout` survived quitToMenu → fired over the menu / TypeError on null mission — `overlayTimer` cleared in resetWorld; (8) mode stacking: entering placement/attack-move didn't clear `nukeTargeting` (L then T then click = accidental 10k⬡ launch) — startPlacing/KeyA/amove all clear it now; (9) gameplay hotkeys fired at the start menu — keydown gates on `started`; (10) mission-granted rig could push a capped player to 21/20 supply and silently stall all production — rig is supply 0 now; (11) enemy starting power plant could land on the home crystal patch (placed before crystals existed) — moved after addPatch; (12) AI refinery expansion bypassed the tech tree via raw aiPlace — routed through aiBuild. Also verified clean: no network calls (pure file://), no hotkey collisions, no localStorage mismatches, no injection paths, resetWorld covers all new state, 8-10 min soaks on all maps/difficulties with zero console errors.
- **Tech tree (2026-07-11, Bronson: "every building needs a benefit"):** `BLD[type].req` = building types that must STAND (built≥1) before placing: barracks←supply, factory←barracks, turret←barracks, airpad←factory, flak←factory, silo←airpad; refinery ungated (economy). `hasTech(team,type)`/`techLabel(type)`; enforced in `startPlacing` (hotkeys + card clicks, toast on locked) and `canPlaceBuilding`; card shows 🔒 + requirement tooltip (cardSig includes tech bits). Pre-placed buildings ignore req — skirmish's starting barracks is grandfathered (2nd barracks needs a depot). **AI obeys the same tree** via `aiBuild()` (recurses to the first missing req; waits — doesn't stack dupes — while the req is under construction; verified rebuild order supply→barracks after base sniping). Bronson's plan: expand to unit-level gates later ("do 3 over time") — e.g. sniper←turret, medic←2 depots — as more units/characters land.
- **Supply Depot repair field (2026-07-11):** depots slowly repair friendly BUILDINGS within `DEPOT_HEAL_RADIUS` (240) at `DEPOT_HEAL_RATE` (0.06/tick vs engineer 0.55) — fields stack, spark fx throttled per building. Green dashed ring on selection + placement ghost. GOTCHA fixed here: dead buildings (hp≤0, pre-filter) must not heal or be healed — without the `hp<=0` guards in updateBuilding (top) and the heal target filter, two dead depots resurrect each other 0→0.06 every tick and the base becomes an unkillable zombie (the death filter only removes hp≤0 at end of tick).
- **Power grid (2026-07-11, C&C brownout):** `BLD` `gen` (HQ 8, Power Plant 10 — new building, 120⬡, hp 400, hotkey O, no req) vs `pow` draw (rax 3, factory 4, airpad 3, turret/flak 2, silo 6; refinery/depot free). `powerMax`/`powerUsed`/`lowPower(team)` count standing living buildings only. Brownout effects: `updateProduction` ×0.5, building `fire()` cooldown ×2 (towers still shoot), `launchNuke` blocked (AI's random launches route through it too). UI: `#res-power` chip (⚡ used/max, warn when low, one-shot toast via `wasLowPower`), blinking ⚡ over browned-out consumers in drawBuilding, `drawPowerBolt` emblem (sprite `bld_power.png` OPT + procedural). Balance intent: HQ's 8 covers mission 1 / early game (rax+turret=5); the first factory (9 total) forces plant #1. Enemy bases spawn ONE plant (ring-search near eHQ via aiSpotFree; 8+10 ≥ their 14 draw); AI rebuilds when browned out — gated to one plant under construction at a time (same stacking gotcha as aiBuild reqs).
- **Repair bays (2026-07-11, paid):** factory repairs GROUND vehicles, airpad repairs FLYERS (`!!u.fly !== (b.type==='airpad')` filter) within `BAY_REPAIR_RADIUS` (200) at `BAY_REPAIR_RATE` (0.5 hp/tick) costing `BAY_REPAIR_COST` (0.15⬡/hp, full tank ≈ 27⬡) — drains `teams[t].crystals`, stops when broke, resumes as income lands. isVehicle covers harvester/rig too (intended). Right-clicking your factory just move-orders vehicles next to it — the aura does the rest, no special order needed.
- **Units:** Harvester (60) · Engineer (90, repairs buildings, trained from HQ, auto-repairs nearby damage when idle) · Marine (80, `H` toggles hunker: ½ damage taken, holds position, still shoots — `order.type==='hunker'`) · Sniper (130, prone silhouette, longest sight) · Raider (150, fast wedge buggy) · Tank (220) · **Artillery (270, 2026-07-09)**: siege RPS counter — `kind:'arc'` shells fly to where the target *was* (no homing, `target:null`) + splash 40 + 1.5× vs buildings; `minRange` 90 dead zone; sight 230 < range 300 so it wants spotters; card slot D (prodKeys now Q/W/E/R/D); can hunker like marines (`canHunker`, 2026-07-09) — ½ damage, holds position, dead zone still applies while dug in. Turret (140) must anchor within `PLACE_NEAR_BASE` of a friendly NON-turret building (anti-turret-creep, playtest feedback). Units wall-slide around buildings (`moveToward` lookahead steer, playtest feedback: was "grinds forever", now ~0.25s overhead).
- **Dinos (2026-07-09, team 3, neutral):** `nest` building (850hp, no team-2 style AI) guards each rich patch; keeps `NEST_BROOD` (3) Spitters alive, respawning one per `NEST_RESPAWN` (7s, clock only runs while short). Spitters use `order.type==='guard'` — aggro within range+90, chase to `NEST_LEASH` (360) of home, walk back; they never leave guard, so artillery beyond aggro range kills nests risk-free (the intended synergy). Fog/targeting code uses `team !== 1` (not `=== 2`) everywhere hostiles are hidden. Dinos draw procedurally (`drawDino`/`drawNest`) even when sprites are on — swap in art later via `drawUnitSprite`.
- **World 96×72 tiles (2026-07-10, was 64×48):** 3072×2304 px — base-to-base ~3250px. MAPS coords are W/H-relative so they scaled automatically; ground texture density scales by area.
- **Terrain (2026-07-10):** `rocks[]` circles from authored `ridges`/`boulders` per MAPS entry, expanded in setup, pre-rendered into the ground canvas (`paintRock`). Impassable to ground units (separation + placement checks); flyers ignore. **Pathfinding**: tile grid (`blocked`, rocks inflated +22px so tanks fit) + LOS fast path + cached A* per order (`o._path`, smoothed). Watchdog: no goal-progress for 2.5s → re-path + `ghostT` 60 (unit briefly ignores rock AND building separation to escape concave pockets — the two-circle pinch and the rock+nest pinch are otherwise inescapable). GOTCHA: map design must keep ridge ends ≥200px from patches/nests — buildings aren't in the path grid, so a ridge ending at a nest creates a blind pocket. Verified 24/24 crossings on all maps (early "failures" were marines eaten by nest guards mid-route).
- **Rocket Trooper (2026-07-10):** Barracks (140). `vehBonus: 1.6` applied at fire() vs `isVehicle` targets; hits air. Barracks now has 6 card slots — prodKeys grew KeyZ (Q/W/E/R/D/Z).
- **APC (2026-07-10):** Factory (200), `cargo: 4`. `order 'board'` (right-click friendly APC with infantry; engineers repair it instead when damaged), `unloadAPC` (key U / card button). Boarded units leave `units[]` but still count supply (`supplyUsed` sums cargo); cargo dies with the APC; pruneSelection/group-recall exclude boarded units (units.includes check).
- **Harrier (2026-07-10):** Airpad (320). `u.armed` + orders `'strike'` (fly to target, one bomb: 120 dmg ×1.4 bld, splash 55) and `'rearm'` (`HARRIER_REARM` 7s at any own airpad). idle/attackmove convert to strike when armed; `commandAttack` routes harriers to strike. AI trains after 9 min. `HARRIER_CAP` 5 per side (alive + queued), enforced in trainUnit; card shows fleet count.
- **Nukes (2026-07-10):** `silo` BLD (500, key N) + `NUKE` spec (tac 10k: r170 dmg1300 hqSafe; hq 25k 'Bunker Buster': r200 dmg3200 kills HQs). `buyNuke`/`launchNuke`/`detonate` (friendly fire on, 45% falloff at rim, tac skips HQs entirely); `NUKE_HQ_EXCLUSION` 180 blocks AIMING tac at an HQ. 30s countdown (`nukes[]`), pulsing world marker + countdown drawn ABOVE fog + blinking minimap dot + 5s warning. Player UX: silo card buys (Q/W), L → `nukeTargeting` click-to-launch (Esc/right-click aborts). AI (diff.aiNukes: hard+specops): builds silo after 8 min, buys busters at 25.5k / tacs at 10.5k, launches within ~25s.
- **Engineer vehicle repair (2026-07-10):** engineers reuse the medic's `'heal'` order with `isVehicle` predicate + `UNIT.engineer.repair` rate (0.55/tick, faster than medic's 0.4). Idle priority: damaged buildings first, then vehicles. Right-click routing: medics take flesh, engineers take steel — one damaged target, both professions can be assigned from one mixed selection.
- **Medic (2026-07-10):** Barracks (100⬡). Unarmed (`dmg:0` + `u.dmg > 0` fire guard), NOT `isCombat` (won't auto-attack; waves send it with a move order). Heals `isFlesh` (infantry + spitters) 0.4hp/tick via `order 'heal'` (auto-acquire when idle, follows target, right-click wounded ally to assign). AI trains up to 2 after 4 min. Draws as engineer sprite + upright red-cross badge; `medic.png` OPT slot for real art. Beware the double-anchor lesson: `} else if (u.type === 'engineer') {` exists in BOTH updateUnit and drawUnit — a careless replace-all once injected canvas code into game logic.
- **Animation pass 1 (2026-07-10, code-only):** `u.walkT/moving` (set in moveToward) → infantry rotation walk-sway (fallback ONLY for units without walk frames — see walk cycles round 2; Bronson dislikes the wobble, it retires unit-by-unit as walk videos land) + spitter scurry wiggle (kept for dinos — reads as scurry); `recoil` on fire (tank/arty 6, towers 4, rest 2.5, decays *0.78) → translate kick for units and the tower gun overlay; ground vehicles emit drive-dust puffs (throttled by `(tick+id)%8`).
- **Hunker pose art (2026-07-10):** `unit_<type>_hunker.png` OPT slots (marine/sniper/artillery) — drawUnitSprite swaps art while `order.type==='hunker'`; missing standing art falls back to hunker art. Snipers now `canHunker` (prone). Current prone sniper art lives in `unit_sniper_hunker.png`; standing art pending.
- **Terrain art hooks (2026-07-10):** `rock.png` (paintRock, deterministic per-rock rotation, natural colors) and `crystal.png` (drawCrystal, keeps glow + depletion scaling) — both fall back to procedural.
- **Optional art slots (2026-07-09):** `OPT`/`opt()` loader — `dino_spitter/dino_nest/gunship/artillery/egg.png` in assets/sprites/ are picked up automatically when present, procedural fallback otherwise (each file independent, unlike the all-or-nothing `bodiesReady`). Spec for Bronson/artists/AI-gen: `assets/sprites/ART-WANTED.md`. New art faces UP; legacy infantry faces right.
- **Alerts (2026-07-09):** `raiseAlert` on damage to team-1 buildings/workers → red pulsing minimap pings (`alerts[]`, 2.5s) + toast/alarm throttled to one per 12s (`lastAlert`).
- **AI base-building (2026-07-09):** every 150 ticks in `aiUpdate` — fixes supply crunches, rebuilds lost barracks/factory/airpad, builds up to 2 flak if the PLAYER fields air, expands up to 3 refineries via `aiExpansionSpot()` (richest live crystal ≥500 from its dropoffs and ≥420 from any nest — the AI deliberately avoids the nest problem). `aiPlace`/`aiSpotFree` mirror canPlaceBuilding minus team-1 rules. GOTCHA fixed here: queued-supply reduce must skip `'up:'` entries (crashed once AI research queued).
- **Pacing (2026-07-09, first pass):** HQ 2200→3000, home patches 1400→1700, neutral fields +400, wave gap floor 45→55s (cadence 82−3/wave). Needs Bronson playtests before more tuning.
- **Flak (2026-07-09):** BLD `flak` (160, hotkey Y, `airOnly:1`) — targets flyers only via `nearestEnemyUnit(...,aa,airOnly)`; same anti-creep anchor rule as turrets (`isDef`); placement ghost shows range ring for any ranged building.
- **Dev mode (2026-07-14, Bronson's request):** hover the `?  controls` chip and tap Space 5x (within 2.5s) — toggles `devMode`: hasTech always true for team 1, crystals pinned ≥99999 every tick, campaign fully unlocked (writes `cc.campaign`), gold tint on the chip while on. Same gesture toggles off (tech re-locks instantly); works at the start menu too. `CC.devMode` for tests. Deliberately undocumented in README — it's his playtest cheat, not a player feature.
- **Stats + polish (2026-07-09):** `stats` {built,lost,kills,mined} → `#ov-stats` scoreboard on the game-over overlay (kills count ANY player-owned killer incl. turrets; losses = player units only). Minimap right-click = move order for selected units. Soft blip on select. Backquote = dev full-map reveal (`devReveal`).
- **Air (2026-07-09):** Gunship (240, Airpad-built; `fly:1`) — flyers skip building wall-slide/collision and only collide with other flyers (`separation`/`moveToward` guards). AA model: `noAA:1` on tank/artillery/harvester/engineer; `canAA(e)`; `nearestEnemyUnit(...aa)` filters flyers; `acquireTarget(x,y,team,range,attacker)`. Force-ordering a noAA unit at a flyer makes it give up (attack case bail). Airpad = BLD `airpad` (175, +2 supply, hotkey X); gunships benefit from Factory vehicle upgrades (IS_INF-based). AI gets an airpad per map (`M.eAir`) and trains gunships after 6 min. Gunship/airpad draw procedurally (rotor anim + ground shadow offset).
- **UI dock (2026-07-09, playtest feedback):** `#dock` = `#qpanel` (queue text + progress + rush buttons, own `lastQSig`) stacked ABOVE `#card` — production starting/finishing never shifts card buttons. Dock-level click delegation handles both panels.
- **Boneyard map (2026-07-10):** 4th map — N/S bases (first vertical axis), twin broken walls at 0.36/0.64H = three N-S gates, mega center (12×3400) with double nest.
- **Static bar (2026-07-10, round 2):** qpanel never hides (idle note when no queue) and both panels have fixed height 118px — bar geometry is constant, verified pixel-identical across production start/finish. Card scrolls (overflow-y) as the narrow-window fallback.
- **Art pipeline v2 (2026-07-10):** generic OPT slots `unit_<type>.png` (ALL units, art faces UP, drawn at r×2.7 team-tinted) and `bld_<type>.png` (all buildings; turret/flak art = base only, game overlays rotating turret_gun; silo warhead drawn on top). Legacy named slots still work as fallbacks. Full Nano Banana prompt pack: `assets/sprites/PROMPTS.md` — one prompt per file + shared style block.
- **Command bar (2026-07-10, playtest feedback):** dock is now a HORIZONTAL bottom strip (left: 220px past the minimap → right edge); card = flex row with `.hd` fixed 170px title block (auto-folded by regex in refreshCard) + buttons flowing sideways. ~100px tall vs the old ~450px column that hid buildings under it. qpanel sits left of the card.
- **Render perf (2026-07-10, playtest lag report):** ground + fog blits are viewport-clipped (was full 3072×2304 every frame — the #1 cost), all entity draw loops cull via `inView` (margins: fx 260, buildings 130), minimap renders every 3rd frame. Sim measured 0.17ms/tick at 92 units — cost is render-side, keep it clipped.
- **Pause & quit (2026-07-10):** `userPaused` (P key / ⏸ chip, `#pause-banner`) gates the sim like the help modal; `quitToMenu()` (⏹ chip, two clicks within 3s) → resetWorld + start menu. Fresh startGame always clears pause.
- **Controls modal + pause (2026-07-09):** `#help` is a centered modal; `paused` gates the sim loop exactly like `!started` (render keeps running). Opens automatically in `startGame` (close = "▶ To battle"), reopens via the ? chip, Esc closes. NOTE `CC.step()` bypasses pause (deliberate, for tests).
- **Spitter cap (2026-07-09):** `SPITTER_CAP` 5 per team, enforced in `hatchSpitter`; card shows `pack n/5`; eggs are never consumed on a refused hatch.
- **Veterancy (2026-07-09):** `u.kills` → `rankOf()` (2/4/8 = Veteran/Elite/Legend, `RANK_AT`/`RANK_NAMES`); +10% dmg / −8% taken per rank folded into `weaponMult`/`armorMult`; rank 3 regens 0.05hp/tick in `updateUnit`. Kill credit flows through `bullet.src` → `damage(e,d,src)`; NOTE this also made idle units retaliate against ranged attackers (bullets used to pass `null` — deliberate improvement, guard-order dinos still don't chase). Gold chevrons via `drawRank`; promotion toast/fx for team 1.
- **Eggs (2026-07-09):** nest death drops `NEST_EGGS` (3) egg pickups (`eggs[]`, `makeEgg`). Harvester-only: right-click egg → `order 'collect'` → carry one (`u.eggCarry`) → `'returnEgg'` to the HQ ONLY (lab flavor) → `teams[t].eggs++`; auto-repeats via `u.lastEggSite` (400 radius at the clutch, else 900 near self). `hatchSpitter(hq)` (HQ card slot R, `cardActions` kind:'hatch') consumes 1 egg + 1 supply, instant spawn, player-controlled team-tinted spitter (`drawDino` uses `COLORS[u.team]`). Topbar 🥚 chip hidden until eggs exist. AI doesn't collect eggs (player-only mechanic, deliberate).
- **Maps (2026-07-09):** `MAPS` table — all-explicit authored layouts (basin = the original, gauntlet = mid-column brawl, valley = diagonal + double-nest mega center). `setup(mapKey)` + `placeBase()` are fully data-driven; add a map by adding a MAPS entry only.
- **Difficulty (2026-07-09):** `DIFFS` table (easy/normal/hard) → `diff.firstWave/waveEvery/capRate/trickle/aiUpgrades`, applied in `aiUpdate`/`waveUpdate`/`startGame`. Normal = pre-difficulty baseline exactly.
- **Start menu (2026-07-09):** `#menu` overlay in index.html; map + difficulty pickers persist to localStorage (`cc.map`/`cc.diff`). `startGame()` calls `resetWorld()` first so double-starts can't stack worlds; `started` gates the sim loop. `CC.startGame(map, diff)` + `CC.step(n)` (runs n ticks synchronously — THE way to test, since rAF sleeps in background tabs).
- **Buildings:** HQ (trains harvester/engineer, supply 20) · Barracks (infantry: marine/sniper, buildable 150) · Factory (vehicles: raider/tank, buildable 200) · Supply Depot (+8 supply, 100) · Refinery (175, harvester drop-off = expansion mechanic; must be placed near a live crystal patch, spawns a free harvester on completion) · Turret (140). Placement hotkeys T/B/V/C/G (`BUILD_MENU`); all but refinery must be within `PLACE_NEAR_BASE` of a friendly building. Supply hard cap 100 (`SUPPLY_HARD_CAP`). Enemy base includes a factory + 2 supply depots; AI runs two production queues (rax=infantry, factory=vehicles).
- **Fog of war:** tile-grid, `explored` (persistent) + `visible` (recomputed every 8 ticks). Toggle `F` / top-bar button: "remembered" (explored stays dimmed) vs "re-fogs" (blacks out when unwatched). Player targeting/clicking respects fog; AI cheats (knows player HQ location).
- **Upgrades (StarCraft-style, 2026-07-09):** `UPG` table + `teams[t].up` levels. Researched at the producing building via the same queue as units (entries `'up:<key>'`): Barracks = infWeapons/infArmor, Factory = vehWeapons/vehArmor, HQ = harvest (carry+speed). Effects via `weaponMult`/`armorMult`/`carryCap`/`effSpeed` — +12% dmg, −10% taken, +3 carry & +10% speed per level, 3 levels each. Card slots Q/W/E/R = `cardActions(b)` (units first, then research). AI researches with spare cash after 5 min. **Rush fees** (`rushProduction`): while a queue is active the card offers 2× speed (fee = half item cost, sets `b.boost=2` for the current item) or finish-now (fee = full item cost); instant respects the supply cap.
- **Game-over feel:** HQ death = triple explosion + shake 20 (normal cap 14, see `addShake`) + double boom; overlay delayed 1.4s and fx/camera keep running behind it (`frame()` else-branch).
- **Enemy AI:** mines its own economy + passive trickle, trains a mixed army capped by a ramp (`3 + 2·minutes` supply — deliberate, keeps wave 1 beatable), keeps 1 engineer after 2 min, waves at 100s then every ~70s, shrinking.
- **Controls:** drag-select, right-click smart orders (move/attack/harvest/repair), `A` attack-move, `S` stop, `H` hunker (marines), control groups `⌃1–5`, arrows/edge/minimap camera (WASD deliberately NOT camera — A/S/W/Q/E/R are command keys), `M` mute.

## Conventions & knobs

- All balance lives in the `UNIT` / `BLD` tables at the top of `game.js` plus `waveAt`/`armyCap` in the AI section. Tweak numbers there; no magic constants buried in logic (or if you add one, hoist it).
- 60 ticks/sec fixed-step loop; times in code are `seconds * 60`.
- `window.CC` is a debug handle (units, buildings, teams, fog, trainUnit, damage, commandMove, waveAt…) — used for automated browser testing; keep it working.
- Team 1 = player (teal), team 2 = enemy (red). `isCombat(u)` excludes harvester + engineer — check its call sites before adding non-combat units.

## Testing / verification

- `node --check game.js` for syntax.
- To drive it in a browser from a Claude session: serve the folder (`python3 -m http.server`) and use the preview tools + `window.CC` for assertions. **Gotcha:** a tool-spawned server may be blocked by macOS TCC from reading `~/Desktop` — if you get 404s on files that exist, copy the folder to the session scratchpad and serve from there.
- No test framework, intentionally — it's a ~3-file toy. Verify by playing/driving the real game.

## Owner context

- Bronson playtests between changes and brings feedback — expect iterative balance/feel requests.
- Open thread: he's comparing the two fog modes ("remembered" vs "re-fogs") across rounds.
- Graphics upgrade (2026-07-08): the game is now fully sprite-based. (1) Sprite *effects* — muzzle flashes, explosion fireballs + smoke, shell impacts, damage fire/smoke on hurt buildings & tanks, mining dust, screen shake (`fxExplosion`/`fxMuzzle`/`fxDamageSmoke`/`fxMinePuff`/`addShake`; knobs in those helpers + `MUZZLE_LEN`/`FX_CAP`). (2) Sprite *bodies* for all units & buildings (`drawUnitSprite`/`drawBuildingSprite`), team-tinted via `teamSprite()`. Procedural drawing remains as automatic fallback for both. Sprite orientation: infantry art faces right (+x), vehicle/turret art points up (drawn with +90° rotation). Next rung if he wants nicer art: commissioned or AI-generated sprites dropped into `assets/sprites/` with the same filenames.
- **Commercial plan (2026-07-08):** Bronson wants to sell this on the Mac App Store — discussed $9.99 (was "with ads" but ads on macOS are impractical/AdMob doesn't support it; steered toward $9.99 no-ads single SKU or IAP tiers). Art budget ~$100 — advice given: keep Kenney CC0 in-game art (free, commercial-safe), spend the budget on a commissioned app icon + store key art (Fiverr). Remember: Apple Developer Program is $99/yr, and shipping needs a native wrapper (WKWebView or Tauri) since it's a browser game.
- **Product direction (2026-07-08, from a Q&A with Bronson):**
  - **Pace:** hobby pace, no release date. Ship when ready.
  - **V1 content:** campaign missions + skirmish (multiple maps, difficulty levels). No survival/challenge modes for now.
  - **Match feel:** 20–30 min games (current game runs shorter — pacing/map size will need tuning as mechanics land).
  - **What he loves:** big army battles + economy/expansion play. Invest polish there first.
  - **Mechanics roadmap (his picks, roughly in build order):** (1) ✅ DONE 2026-07-08: Factory (Barracks=infantry, Factory=vehicles), Supply Depot (+8, cap 100), and Refinery expansions (he came around on these — "a huge piece of this"). (2) ✅ DONE 2026-07-09: artillery RPS unit + dino nest creeps + 3 maps + difficulty levels (the "playability sprint"). (3) NEXT: the pacing pass toward 20–30 min matches — everything that changes match length is now in, so tuning won't have to be redone.
  - **Campaign flavor:** light sci-fi story — short mission briefings, no cutscenes. UPDATED 2026-07-11: Bronson now wants a **~20-mission arc** (was 8–10) with a locked story beat: **the red team loses the human war — dinos overrun their camp mid-campaign — and the dino-occupied red faction becomes the enemy for the back half.** Fits the "tease then deliver" ship gate (Act 1 human-vs-human can ship in v1; dino acts land as the free update). Next design steps discussed 2026-07-11: per-team color schemes (COLORS/teamSprite already centralize tinting) and expanding the dino roster (~3–4 more species; remember: engine has no melee system — fake melee = very short range). 2026-07-11: mission framework + mission 1 "Landfall" SHIPPED (see snapshot); Bronson's briefing wishlist: "a short 30-second video in a perfect world" — full-screen text briefing chosen for now, video briefings are a someday-nice-to-have.
  - **PvP:** explicitly parked ("not sure yet") — decide after single-player ships. Don't architect for it yet.
- **THE HOOK — dinosaurs (2026-07-08, deep design Q&A):** Bronson's idea, validated and committed: humans vs dinosaurs, Turok/Jurassic Park energy. This is now the store pitch ("classic RTS meets Jurassic Park"), not a side feature. Decisions from the Q&A:
  - **Keep the name Crystal Command; reframe fiction:** mining the crystals is what wakes/enrages the planet's native dinos. Campaign = human expedition (player) vs escalating dino threat.
  - **Dino scope, in order:** (1) neutral wildlife first — **the "nest problem" is the signature moment**: rich crystal patches guarded by a dino nest (respawns defenders until destroyed) + spitter dinos; clear it or mine poor. Integrates directly with the refinery/expansion economy. (2) Campaign antagonist faction (AI-only — no dino economy/balance needed). (3) Full playable dino faction only if the game earns it — do NOT start here.
  - **Combat:** start with ranged-ish dinos (spitters) + at most one simple melee charger; build real melee systems (charge/surround/swarm) later. Engine currently has zero melee support — remember this before promising raptor packs.
  - **V1 ship gate:** "tease then deliver" — v1 = skirmish (with nest creeps) + early campaign missions; dino campaign arc lands as a free update.
  - **Audience:** nostalgic RTS 30–50 AND dads-playing-with-kids — keep violence cartoonish (Kenney style already is), difficulty options matter.
  - **Success metric:** craft & hobby first — shipping matters but there is no launch-window pressure; protect the fun of building it.
  - **Dino art:** hunt itch/CC0 top-down dino packs first; if nothing matches, Bronson AI-generates sprites and art-directs, dropped into the existing same-filename sprite pipeline. Kenney has no dinos.
  - **Design synergy to exploit:** nests demand a siege answer → the planned artillery unit becomes the nest-cracker → human counter-unit step and dino step reinforce each other.
- Feature ideas floated: more buildings (supply/factory), air units, hero units, repair costing crystals if too forgiving.
- **Keep this project fully separate from Point Bandit** (his credit-card rewards app). No cross-references, no shared config.
